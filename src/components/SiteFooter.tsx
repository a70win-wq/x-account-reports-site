export function SiteFooter() {
  return (
    <footer className="mt-auto border-t border-rule">
      <div className="mx-auto flex max-w-6xl flex-col gap-2 px-5 py-8 text-sm text-muted sm:px-8 sm:flex-row sm:items-center sm:justify-between">
        <p>每日自找 X 帳號全析 · 只讀公開資料</p>
        <p>數字出自各篇原文，本站不另算、不即時抓取。</p>
      </div>
    </footer>
  );
}
